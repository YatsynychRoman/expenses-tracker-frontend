import React from 'react';

import Content from '../../modules/Register/components/Content';

const RegisterViewComponent = (): React.ReactElement => <Content />;

export default RegisterViewComponent;
